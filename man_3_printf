NAME 
	printf

SYNOPSIS
	#include <stdlib.h>
	#include <unistd.h>
	#include <stdarg.h>
	int _printf(const char *format, ...);
DESCRIPTION
	The '_printf' function is very similar to the printf function from the standard c library.
	it is a variadic function that accepts as argument the format string (*format) and different
	data types.
RETURN VALUE
		
